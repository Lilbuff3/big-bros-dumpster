{
  "name": "big-bros-dumpster",
  "version": "1.0.0",
  "private": true,
  "description": "Big Bros Dumpster Rentals — static site",
  "scripts": {
    "build": "tailwindcss -i css/tailwind.src.css -o css/tailwind.css --minify",
    "watch": "tailwindcss -i css/tailwind.src.css -o css/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.3",
    "tailwindcss": "^4.2.3"
  }
}
